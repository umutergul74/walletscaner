name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: walletscaner_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d walletscaner_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/walletscaner_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends zstd
      - run: npm ci
      - run: npm run test:agent-tooling
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build --workspaces --if-present
